const express = require('express')
const Parser = require('parser')
const app = express()


app.use(parser())


app.get('/numbers',res) => {
  const schedule = ["ownerName", "rollNo", "ownerEmail","acesscode"]
  res.json(number)
})

